---
wave: 1
depends_on: []
files_modified: [app.py, routes/super_admin.py, config.py]
autonomous: true
---

# Plan: Foundation & Routing

This plan establishes the Super Admin route structure and secures access to global analytics.

## Tasks

<task>
<read_first>
- `app.py`
- `routes/auth.py`
- `config.py`
</read_first>
<action>
Create `routes/super_admin.py` and register the `super_admin_bp` blueprint.
Implement a `before_request` hook in the blueprint to verify that the logged-in user has `admin_type == 'super'`.
Update `app.py` to register the new blueprint.
</action>
<acceptance_criteria>
- `routes/super_admin.py` exists and defines `super_admin_bp`.
- Accessing `/super_admin/` without a super admin session redirects to login or returns 403.
- `app.py` contains `app.register_blueprint(super_admin_bp, url_prefix='/super_admin')`.
</acceptance_criteria>
</task>

<task>
<read_first>
- `templates/base.html`
</read_first>
<action>
Create a base skeleton for the God's Eye dashboard at `templates/super_admin/dashboard.html`.
Update the sidebar in `base.html` to conditionally show a link to the "God's Eye Dashboard" only for users with `session['admin_type'] == 'super'`.
</action>
<acceptance_criteria>
- `templates/super_admin/dashboard.html` exists with basic "God's Eye Dashboard" title.
- Sidebar link is visible only to Super Admin users.
</acceptance_criteria>
</task>

## Verification Criteria

- [ ] Super admin login works and reveals the "God's Eye" link.
- [ ] Standard campus admin login does NOT reveal the "God's Eye" link.
- [ ] Direct URL access to `/super_admin/` is blocked for non-super admins.

## must_haves

- `super_admin_bp` registered and secured.
- "God's Eye" navigation link restricted to Super Admin.
