---
wave: 1
depends_on: []
files_modified: [templates/super_admin/dashboard.html, routes/super_admin.py, static/css/style.css]
autonomous: true
---

# Plan: God Eye Dashboard UI

This plan implements the visual structure of the global dashboard with real-time KPI cards.

## Tasks

<task>
<read_first>
- `routes/super_admin.py`
- `services/branch_queries.py`
</read_first>
<action>
Update the `god_eye()` route in `routes/super_admin.py` to:
1. Fetch branch summaries using `get_all_branches_summary()`.
2. Fetch global aggregate using `get_global_aggregate(summaries)`.
3. Pass both to the template.
</action>
<acceptance_criteria>
- `god_eye()` passes `summaries` and `aggregate` objects to the template.
- Errors in fetching are handled gracefully.
</acceptance_criteria>
</task>

<task>
<read_first>
- `templates/super_admin/dashboard.html`
- `templates/base.html`
</read_first>
<action>
Implement the full dashboard UI in `templates/super_admin/dashboard.html`:
1. **Global KPI Row**: Cards for Total Patrons, Active AY Patrons, Total Issues, Overdues.
2. **Branding**: Use the golden-brown color scheme (#C5A059, #8B6914).
3. **Animations**: Add hover effects and entrance animations to cards.
</action>
<acceptance_criteria>
- Dashboard shows 4-5 global aggregate cards at the top.
- Designs are premium with smooth gradients and hover effects.
</acceptance_criteria>
</task>

## Verification Criteria

- [ ] Global KPIs reflect the sum of data from all online branches.
- [ ] Hovering over cards triggers a smooth visual transition.
- [ ] Dashboard is responsive on tablet/mobile views.

## must_haves

- Real-time aggregation displayed on page load.
- Institutional golden-brown theme application.
