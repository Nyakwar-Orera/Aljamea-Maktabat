---
wave: 2
depends_on: [03-01]
files_modified: [templates/components/popups.html, static/js/dashboard.js, templates/super_admin/dashboard.html]
autonomous: true
---

# Plan: Specialized Reports & Modals

This plan implements the interactive depth of the dashboard, including specialized language/fiction reports and the "Chart-to-Table" popup logic.

## Tasks

<task>
<read_first>
- `services/branch_queries.py`
- `services/koha_queries.py`
</read_first>
<action>
Implement `get_global_language_distribution()` and `get_global_fiction_stats()`.
Aggregate these stats across all branches in the Super Admin service layer.
</action>
<acceptance_criteria>
- Global language breakdown (Arabic, English, etc.) is available.
- Fiction vs Non-Fiction global ratio is calculated.
</acceptance_criteria>
</task>

<task>
<read_first>
- `templates/base.html`
</read_first>
<action>
Create `templates/components/popups.html` for reusable data modals.
Implement a global Javascript handler in `static/js/dashboard.js` that:
1. Listens for clicks on any Chart.js instance.
2. Formats the underlying data into a HTML table.
3. Opens the modal with a `DataTable` instance for easy searching.
</action>
<acceptance_criteria>
- Clicking any chart on the Super Admin dashboard opens a modal.
- The modal contains a searchable table of the data shown in that chart segment.
</acceptance_criteria>
</task>

## Verification Criteria

- [ ] Language distribution chart accurately reflects total global stock.
- [ ] Clicking a bar in the Top Books chart opens a modal with a list of branches where those books were issued.
- [ ] Popup tables support CSV-like granularity.

## must_haves

- Chart.js click interaction.
- Reusable Modal component.
- Multi-campus language/category aggregation.
