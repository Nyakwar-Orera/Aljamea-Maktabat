---
wave: 1
depends_on: []
files_modified: [services/branch_queries.py, templates/super_admin/dashboard.html, routes/super_admin.py]
autonomous: true
---

# Plan: Global Stock & Top Books

This plan implements global title aggregation and the specialized reporting for the top-performing library assets.

## Tasks

<task>
<read_first>
- `services/branch_queries.py`
</read_first>
<action>
Implement `get_global_top_titles()` in `services/branch_queries.py`.
It should:
1. Parallel fetch top titles from all branches.
2. Deduplicate and aggregate counts.
3. Return the top 10 global performers.
</action>
<acceptance_criteria>
- Function correctly sums issue counts for identical titles across campuses.
- Returns a list of the top 10 unique titles globally.
</acceptance_criteria>
</task>

<task>
<read_first>
- `templates/super_admin/dashboard.html`
</read_first>
<action>
Add the "Global Collection Excellence" section to the dashboard:
1. Top 10 Books chart (Horizontal Bar).
2. "Highest Circulating" insights box.
3. Apply golden-brown styling to these sections.
</action>
<acceptance_criteria>
- Dashboard includes a Top 10 Books visualization.
- Analytics match the aggregated global data.
</acceptance_criteria>
</task>

## Verification Criteria

- [ ] Global Top 10 contains books borrowed across multiple campuses.
- [ ] No duplicate titles appear in the global list.
- [ ] UI is visually consistent with Phase 2 cards.

## must_haves

- Accurate cross-campus title aggregation.
- Responsive chart visualization.
